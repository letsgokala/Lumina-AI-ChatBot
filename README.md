# Lumina AI Workspace

A premium chat workspace for experimenting with modern AI interactions in the browser.

## Stack

- React 19
- Vite
- TypeScript
- Tailwind CSS

## Development

1. Install dependencies with `npm install`
2. Copy `.env.example` to `.env` and add your Gemini API key
3. Start the dev server with `npm run dev`
